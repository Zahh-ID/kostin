# AuthController

**Namespace**: `App\Http\Controllers\Web`

## Description
Controller for handling requests related to AuthController.

## Methods
- `showLoginForm`: Handles the `showLoginForm` action.
- `login`: Handles the `login` action.
- `showRegisterForm`: Handles the `showRegisterForm` action.
- `register`: Handles the `register` action.
- `logout`: Handles the `logout` action.
